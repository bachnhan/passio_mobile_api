@model HmsService.ViewModels.CostCategoryViewModel

<form action="@Url.Action("Edit","CostManage")" method="POST">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title">Cập nhật nhóm doanh thu chi</h4>
    </div>
    @Html.HiddenFor(m => m.CatID)
    <div class="modal-body">
        <div class="form-horizontal">
            <div class="form-group">
                <label class="col-sm-4 control-label">
                    Tên mục
                </label>
                <div class="col-sm-8">
                    @Html.TextBoxFor(model => model.CatName, new { @class = "form-control datepicker" })
                    @Html.ValidationMessageFor(model => model.CatName)
                </div>
            </div>
            <div class="form-group">
                <label class="col-sm-4 control-label">
                    Loại
                </label>
                <div class="col-sm-8">
                    <div class="select">
                        @Html.DropDownListFor(model => model.Type, this.Model.ListType, new { @class = "form-control" })
                    </div>

                </div>
            </div>
        </div>
    </div>
    <div class="modal-footer">
        <button type="submit" onclick="return validateEdit(this)" class="btn btn-primary">Cập nhật</button>
    </div>
</form>

<script type="text/javascript">
    function validateEdit(btn) {
        hasValue = true;
        if ($("input[name='CatName']").val() == "") {
            ShowMessage("Vui lòng nhập tên mục.", 3);
            hasValue = false;;
        }
        return hasValue;
    }
</script>


